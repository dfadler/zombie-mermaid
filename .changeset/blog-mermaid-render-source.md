---
---

No release: a `mermaid-render` fence in a blog post now renders its
syntax-highlighted source followed by the rendered SVG diagram, back to
back, instead of the diagram alone. Plain `mermaid` fences are unchanged.
Nothing here touches the published `zombie-mermaid` package.
