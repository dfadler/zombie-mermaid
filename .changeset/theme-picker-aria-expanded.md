---
'zombie-mermaid': patch
---

Demo site: expose the theme picker's "More themes" dropdown state via `aria-haspopup`/`aria-expanded`/`aria-controls`, matching the pattern already used by the sidebar toggle. No library API changes.
