---
---

No release: prep work for #625 (umbrella #620) — pnpm workspace tooling (`packages/*` glob, tsconfig/vitest/eslint coverage, the diff-coverage tool) and doc updates ahead of the actual `core` package move, split out to keep that PR's file count under CodeRabbit's review limit. No `packages/*` directory exists yet, so these glob patterns match nothing and change no build output.
