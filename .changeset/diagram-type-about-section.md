---
---

No release: adds an "About this notation" section to each of the 6
diagram-type pages (`diagrams/<type>.html`) -- a longer, syntax-specific
paragraph on that diagram type, paraphrased from Mermaid's own docs, plus
an outbound link back to the real syntax reference page it was drawn from.
Sits between "Source → render" and "Every example". Nothing here touches
the published `zombie-mermaid` package.
