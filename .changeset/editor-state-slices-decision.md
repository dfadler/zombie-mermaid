---
---

No release: adds a `docs/decisions/` entry recording why the editor's
`EditorState`/`EditorAction`/`editorReducer` slice split was investigated
and not adopted. Docs-only, no published package is affected.
