---
---

No release: adds `demo/components/theme-bar-client.ts`, the interactive
controller for the global `ThemeBar`/`ThemePicker` (#686). Demo/site-only —
not wired into any page yet (that's #687) and doesn't touch the published
`zombie-mermaid` package, so there's no consumer-facing change to describe.
