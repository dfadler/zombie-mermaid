---
'zombie-mermaid': patch
---

Demo site: on the per-diagram-type pages (`/diagrams/<type>.html`), the Mermaid source panel now sits to the left of the rendered diagram instead of below it. Side by side above 720px viewport width, stacked (source above diagram) below it.
