---
---

No release: relocates `hero.svg`, `logo-lockup-dark.svg`, and
`logo-lockup-light.svg` from the repo root into `public/` for #999. Static
asset move only — file content is unchanged and no published package is
affected.
