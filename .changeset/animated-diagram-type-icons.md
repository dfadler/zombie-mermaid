---
'zombie-mermaid': patch
---

Add the six animated diagram-type icon components (flowchart, state, sequence, class, ER, XY chart) to the demo's icon set, each respecting `prefers-reduced-motion: reduce`. Refs #597.
