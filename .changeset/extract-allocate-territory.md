---
---

Extract the ASCII class diagram's label territory allocation into a standalone,
storage-agnostic `allocateTerritory` utility (`src/ascii/territory.ts`), now
independently unit tested. Pure refactor — rendered output is unchanged. Refs #618.
