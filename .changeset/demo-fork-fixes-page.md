---
---

Demo site only: add a "What this fork fixes" page showing before/after renders for 13 bugs this fork fixed over upstream. Each "before" is rendered by the project's own renderer at the commit immediately preceding the fix; the generator fails the build if any pair renders identically. No published package change.
