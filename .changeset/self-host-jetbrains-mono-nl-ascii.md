---
---

No release: self-hosts a subsetted JetBrains Mono NL for the demo site's
ASCII output (#978) — adds `subset-font` as a devDependency and a
`build:mono-font` script, touching `package.json`. No published package
(`src/**`, `packages/**`) changes.
