---
---

No release: adds `demo/theme-state.ts`, a shared theme-state module for the
site's demo pages, plus an architecture decision doc (#685). Nothing here
touches the published `zombie-mermaid` package — the new module isn't wired
into any page yet (that's #686/#687), so there's no consumer-facing change
to describe.
