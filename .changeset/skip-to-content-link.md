---
'zombie-mermaid': patch
---

Demo site: added a "Skip to samples" link as the first focusable element on the page, hidden off-screen until it receives keyboard focus, so a keyboard-only visitor can jump straight past the top nav and sidebar to the sample list instead of tabbing through the full nav on every page load. No library API changes.
