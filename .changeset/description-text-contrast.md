---
'zombie-mermaid': patch
---

Demo site: darken the description caption text under each sample's title so it meets WCAG AA's 4.5:1 contrast minimum for 16px body text. The shared muted-text token it draws from is used site-wide across all 16 bundled themes. No library API changes.
