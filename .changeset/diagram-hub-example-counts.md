---
---

No release: part 3 (final) of #989. The diagrams hub (`diagrams/index.html`)
now shows an "N examples" pill next to each type's "View examples" CTA --
the real per-type sample count (`allExamplesFor(slug).length`), so a
visitor knows how many dedicated pages they'll find before clicking
through. Nothing here touches the published `zombie-mermaid` package.
